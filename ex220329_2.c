#include <stdio.h>
/*
typedef struct student {
	int number;
	char name[10];
	struct date* dob;
	struct student* ss;
}stu;
struct date {
	int month;
	int day;
	int year;
};

void main() {
	typedef struct date newdate;
	newdate d = { 10,5,1999 };
	stu s = { 2022001,"kimsuk" };

	s.dob = &d;
	printf("학번: %d\n", s.number);
	printf("이름: %s\n", s.name);
	printf("%d년 %d월 %d일 생일\n", s.dob->year, s.dob->month, s.dob->day);
}
*/