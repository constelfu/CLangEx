/*
#include <stdio.h>
#define SQUARE(x) ((x)*(x))				//괄호를 써야 오류 안 남
#define ABS(x) ((x)<0 ? (-x):(x))

void main() {
	int n;
	puts("숫자입력: \n");
	scanf_s("%d", &n);
	printf("제곱: %d\n", SQUARE(n));
	printf("절대값: %d\n", ABS(n));
}
*/