#include <stdio.h>

int main() {
	/*
	int num;

	printf("정수 입력: ");
	scanf_s("%d", &num);
	
	if (num > 0) {
		printf("%d은(는) 양수입니다\n", num);
	}
	else if (num == 0) {
		printf("%d은(는) 0입니다\n", num);

	}
	else {
		printf("%d은(는) 음수입니다\n", num);
	}
	*/

	/*
	int score;

	printf("점수 입력: ");
	scanf_s("%d", &score);
	
	if ((score >= 60) && (score < 90)) {
		printf("%d점입니다 수료가능합니다", score);
	}
	else if (score >= 90) {
		printf("%d점입니다 장학금대상입니다", score);
	}
	else {
		printf("%d점입니다 낙제입니다\n", score);
	}
	*/

	/*
	int x = 0;

	printf("정수입력: ");
	scanf_s("%d", &x);
	
	if ( x ) {
		printf("입력된 숫자는 0이 아님\n", x);
	}
	*/

	/*
	int x, y;

	printf("첫번째 수 = ");
	scanf_s("%d", &x);
	printf("두번째 수 = ");
	scanf_s("%d", &y);

	//printf("큰 수 = %d\n", (x > y) ? x : y);
	//printf("작은 수 = %d\n", (x < y) ? x : y);

	if (x > y) {
		printf("큰 수 = %d\n작은 수 = %d\n", x, y);
	}
	else if (x == y) {
		printf("두 수는 같은 수입니다");
	}
	else {
		printf("큰 수 = %d\n작은 수 = %d\n", y, x);
	}
	*/


	return 0;
}